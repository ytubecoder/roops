# runtime state — never committed
.env
state/
reports/
dashboard/loops.html
dashboard/.loops-dashboard-*.tmp
dashboard/reports.html
launchd/*.plist

# machine-local — the kagami name-leak denylist never ships in the repo
loops.d/kagami/leak-terms.local.txt

# scratch / tooling
.playwright-mcp/
.superpowers/
__pycache__/
*.pyc
.DS_Store

# generated by ticket-takeaway
docs/sdlc-dashboard.html
