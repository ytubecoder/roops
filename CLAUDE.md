# Roops — recurring automation harness (mechanical name: loops)

Custom thin harness for scheduled agent "loops" (recurring maintenance/monitoring jobs across ~/projects). **Status: BUILT + live-verified 2026-07-22** (real launchd firing, codex AND claude, findings memory/dispositions, enforcement denial, full dashboard matrix; 1028 hermetic tests via `bash tests/run-tests.sh` — 661 python + 367 shell, measured 2026-08-02).

**Fleet state (2026-08-03):** three loops installed to launchd — `loop-sensei` (the fleet examiner — diagnoses failed loops, proposes fixes as findings; `daily:20:00` local), `kagi-ban` (CLI-secrets exposure audit, the first page-enabled loop; `daily:07:40`), and `kagami` (鏡 — public UI-mockup mirror, B-18; `daily:21:30`; SHAPE-MIRRORS the live fleet into mock data (`fixture/build_root.py --source` — shape crosses, strings/values never) and renders it with the REAL `dashboard/generate.py --now`, gates it (self-contained / name-leak / token-drift / parity — B-20 2026-08-03: the real garden's rendered feature surface must ⊆ the mirror's, so "success" can never mean a mirror that under-exhibits the current interface), byte-compares the live `roops/ui.html` (which REPLACED the hand-made concept page 2026-08-03), and on drift opens/updates ONE PR on `ytubecoder/ytubecoder.github.io` — merge is the approval gate; the remote mutation lives in trusted precheck code, engine at the report-only floor; public-facing strings say "roops mirror", never the internal loop name). Everything else is supervised-only. Install state is machine-local: `launchd/*.plist` is gitignored, so a fresh clone shows installed=False for everything until reinstalled.

**Manual run trigger (B-13, shipped 2026-08-03):** every loop row on the garden carries a 走 run-now button when served via `loopctl serve` — `POST /api/loops/<name>/run` fires one supervised run in a console background worker, deliberately NOT gated on installed/enabled (that's the point: ads loops run on demand while launchd install stays blocked). Contract: `docs/INTERFACES.md` §13.3 — the ONLY console path that fires a run; install/uninstall stay CLI-only (§8.1). OpenSpec change `b-13-dashboard-run-now-2026-08-02` (built as the first test-gated peon dispatch: foreman-authored red suite → scoped codex peons → foreman-run verify).

**Naming new loops:** follow the roops Japanese theme (brand lives in `site/`): loop-sensei, not loop-doctor.

## Start here (cold start)
- **Building a LOOP:** `docs/LOOP_AUTHORING.md` + `bin/loopctl new` — run the intake interview first (12 questions, in the doc — q12 is the optional report page); fill SPEC.md; `loopctl validate` gates it.
- **Converting an existing Agent Skill into a loop:** `docs/SKILL_IMPORT.md` + `loopctl import <skill-path> --analyze/--apply` — static, zero-token gap analysis that scaffolds a loop from a `SKILL.md`; same gates as hand-authored loops (validate → supervised run → install). Discoverability layer for Claude-family agents: `skills/loops/SKILL.md`.
- **Touching HARNESS code:** `docs/INTERFACES.md` is the frozen mechanical contract — conform or amend it explicitly, never drift. Design rationale: `docs/HARNESS_PLAN.md` + `docs/HARNESS_PLAN_AMENDMENT_1.md` (do not relitigate settled decisions). Verified engine CLI facts: `docs/ENGINE_PROBES.md`.
- **Choosing/specifying loops:** work from `docs/LOOPS_WARMSTART.md`.
- **Ads loops (five per-network checks + the `/ads/actions` console surface):** `docs/ADS_LOOPS_FOLLOWUP_WARMSTART.md` — current state, the three open issues with their re-check commands, and the acceptance bar (a manual "run everything" trigger is phase 1; launchd scheduling is phase 2, explicitly not before).
- **Report pages (third output tier, SHIPPED 2026-07-30 with pilot `kagi-ban`):** author guide `docs/REPORT_PAGES.md`; contract `docs/INTERFACES.md` §4.1 step 6.5 + §12. Nothing is outstanding — the parked decisions, the follow-up backlog, and both trailing leftovers were **cleared 2026-07-30**; `docs/REPORT_PAGES_FOLLOWUP_WARMSTART.md` now holds live state, the "why 16 not 20" PATH explanation (do NOT restore the 20), and the settled-do-not-relitigate list. Renderer KV-neutralization is single-sourced from `bin/redact.py`'s exported `KV_*` constants — a `\b`-vs-lookbehind divergence there was silently failing the promotion gate on underscore compounds (`GITHUB_TOKEN=`); `pagekit/kit.css` is now the single source of report-page CSS — renderers READ it via `$PAGEKIT` and inline the body (pages stay self-contained; a missing kit.css fails the render rather than shipping unstyled), so editing it restyles every page-enabled loop; since 2026-08-02 it is built on the garden's shared tokens (next bullet).
- **Garden three-tier reorg (SHIPPED 2026-08-02, B-14..B-16):** umbrella spec `docs/superpowers/specs/2026-08-02-garden-three-tier-design.md` (settled decisions — do not relitigate); WP briefs in `docs/workpackages/`. The dashboard is garden-only: each row is a `<details name="garden">` accordion (summary = glance tier, body = findings/runs/report block), long tier = the loop's own report page. `dashboard/reports.html` is RETIRED — generator doesn't emit it, console 404s it, machine-local Caddy falls back to the dashboard via `try_files`; do not resurrect it. Load-bearing invariants: (1) `dashboard/generate.py`'s token blocks are the CANONICAL palette (light + dark, 13 role tokens + 4 `-rgb` companions); `pagekit/kit.css` copies them verbatim and `tests/test_token_drift.py` fails on any divergence — a value change in one file must ship with the other in the same change; (2) token blocks stay ONE `--token: value;` declaration per line (the drift test is a dumb text parser BY DESIGN — don't reformat); (3) theme persistence is pinned: localStorage key `loops-theme`, attribute `data-theme` on `<html>`, values `dark`/`light`, shared verbatim by garden and report pages so the choice carries across; (4) fonts (`--serif`/`--mono`) live in the base `:root` only, never in dark blocks. Since B-19 (2026-08-03, INTERFACES §10): filter/sort controls live in the garden kicker (glossary note removed), and DEFAULT row order is recency = the loop's newest lifecycle event of ANY type (server-rendered; event-less pre-B-18 loops trail in name order until first touched) with FLIP-animated client re-sort. Parked (recorded in the umbrella, not spec'd): orphaned-report-dir indexing, print expand-all.
- **Loop ownership (B-17, SHIPPED 2026-08-03):** every `loop.conf` carries `owner=<project>` — required-but-assumed: absence never fails anywhere, it resolves to `loops` and renders visibly "assumed" (INTERFACES §5 owner-resolution block is the authority; §8 set-owner/`--owner`; §10 garden chip+filter). Load-bearing invariant: `loopconf.resolve_owner()` is CANONICAL and `dashboard/generate.py` carries a lockstep mirror (lazy-seam doctrine — generate must run without bin/loopconf.py on disk) pinned by a drift test in `tests/test_dashboard.py` — a rule change must ship in both files together, same pattern as the token blocks. Edit owners via `loopctl set-owner` (garden 主 chip click copies the command); scaffold with `loopctl new --owner <project>`. Fleet: ads-* → maguyva-marketing, everything else → loops.
- **Open threads / unfinished design work:** `docs/OPEN_THREADS_WARMSTART.md` — main open thread is the approve→action bridge (ack ≠ approval; DMP id-space gap; Google Ads OAuth blocker). Check it before assuming a thread is settled; delete sections there as they resolve.
- **Roops rebrand (applied at brand level 2026-07-30):** GitHub repo renamed `ytubecoder/roops`; README + dashboard header/title say roops; `bin/roopctl` aliases `loopctl`. Mechanical names deliberately unchanged per the settled alias-not-a-rewrite rule: `loopctl`, `loops.d/`, `state/loops.sqlite`, `com.loops.*` launchd labels, INTERFACES vocabulary, and this machine's checkout stays at `~/projects/loops` (installed plists reference it). The former standalone site repo is retired (`roops-retired`, pending deletion — gh token lacks `delete_repo` scope); brand + UI-concept pages live in `site/` with full history (design system in `site/CLAUDE.md`). The site is LIVE at https://ytubecoder.github.io/roops/ — served from the public `ytubecoder/ytubecoder.github.io` repo (`roops/` folder; user-pages workaround because Pages on the private harness repo needs a paid plan). `site/` is the source of truth, publish via `site/workflows/publish.txt`; published pages stay mock-data-only BY RULE — the real dashboard, with real loop names and spend, is a local gitignored artifact and is never published. `dashboard/generate.py` renders the garden design (hanko stamps over unchanged §4.3 precedence, per-loop tokonoma, pancake chips, 巡/休 install state — B-07; §10 amended thrice: style, staleness, paused-staleness resolved (paused loops stay stale-visible; `set-schedule manual` is the exempt off-path)). Remaining concept follow-ups (interactive hanko/copy buttons, fleet ledger zone, live-run tail, `niwashi` gardener loop) — each visual one is done or presentation-only; the rest still need INTERFACES amendments.
- Findings/disposition flow (the human arrow): `loopctl findings <loop>` → `ack|dismiss --note|snooze --until|reopen`. Dismissed/snoozed findings are suppressed by the RUNNER (latest.json + dashboard); the engine still emits them; audit copy stays in `state/runs/<id>/contract.json`.

## Change lifecycle routing (which flow for which change)
- **Feature-scale change** (new capability, new loop, new output tier — anything that IS or
  should be a B-ticket): OpenSpec is the enrolled spec lifecycle. `openspec-propose` creates
  `openspec/changes/<ticket-id>-<slug>-<date>/` — ticket id FIRST; the CLI rejects names
  starting with a digit, so `<date>-...` fails (hit 2026-08-03). Implement via
  `openspec-apply-change`; `openspec-archive-change` when done (archive prefixes the date).
  Precedent: B-01, B-17 (B-04..B-08 bypassed it via superpowers plans; feature-scale work
  goes through OpenSpec).
  `docs/INTERFACES.md` stays the frozen authority — OpenSpec artifacts point at it, never
  duplicate it (`openspec/config.yaml` says the same).
- **Small fix / hardening / docs amendment** (like B-09): superpowers plan in
  `docs/superpowers/plans/` or a direct edit; the INTERFACES amendment ships in the same
  commit as the code. No OpenSpec change below feature scale.
- **Either way:** open a Ticket Takeaway ticket at start of work
  (`python3 ~/.claude/ticket-takeaway/tickets-cli.py add loops "<title>" --section wip`),
  move it to review when coding completes.

## Non-negotiables (from the plan — full rationale in docs/HARNESS_PLAN.md)
- Engine: **codex default** (`codex exec --output-last-message --output-schema`), claude switchable (`claude -p --json-schema`), local models later. Prompts engine-neutral; prefer CLI/curl over MCP.
- The real invariant (README "What Can Actually Change Things"): **deterministic code you wrote gets full power; the model gets a sandbox.** `precheck.sh` is trusted UNSANDBOXED bash (all network/file I/O belongs there); the **model** is contained by the four per-loop permission axes, never by prompt text alone. The whole current fleet sits at/near the report-only floor; widening an axis is a per-loop config change requiring written justification — never something inherited or assumed. Know which axis actually contains what: the **working-directory write sandbox**, `--tools`, and `perm_network=none` are the real containment. `exec_allowlist` is NOT — a non-allowlisted `echo` still ran under a one-entry allowlist (verified 2026-07-28), so treat the allowlist as intent, not as a boundary.
- macOS has **no `flock`, no GNU `timeout`** — use the fcntl lock helper + runner-owned process-group timeouts.
- Paperclip and hermes are OUT as harness (evaluated + rejected 2026-07; hermes cron is the documented fallback; `hermes` ≠ `hermes-local`). Never install `NousResearch/hermes-paperclip-adapter` v0.3.0 (frozen, buggy).
- All paths `$HOME`-relative (runs on macOS now, WSL later).

## Critical gotchas (cost real debugging time — the WHY matters)
- **A loop cannot declare its own displayed status when it emits findings.** Per `INTERFACES.md` §4.5, a NON-empty `findings` array makes `effective_status` = max severity of the unsuppressed findings and **discards the declared `status`**; only an EMPTY `findings` lets `status` through. A run that failed to write its action set declared `alert` and displayed **amber**, because its findings topped out at `warn`. If a run must surface red, it emits zero findings.
- **The engine's shell layer hard-denies any command containing both a brace and a quote** ("expansion obfuscation") — before the script runs, so the script cannot detect it. Any loop whose engine writes structured data needs a brace-free payload format. Full probe table + the allowed/denied pair: `docs/ENGINE_PROBES.md`. Do NOT let a model invent smuggling workarounds (`tr`, hex escapes, process substitution); the correct response to a denial is to remove the brace.
- **Model-emitted metrics get believed.** A run reported `inputs.missing: 4` while its own digest showed all four inputs healthy, tripping the dashboard's alert threshold on good data. Metrics a precheck can compute should be computed there and copied verbatim, never left to the model.
- **A plist file in `launchd/` does not mean installed.** `loopctl` checks plist + `launchctl print`; the dashboard (§10 amendment 2026-07-30) checks file presence only, to stay subprocess-free and hermetic. Five ads-* plists existed with nothing bootstrapped (written 2026-07-28, never loaded — ads scheduling is explicitly phase 2 per B-03), making the garden show 巡/stale for loops that would never fire. Orphaned plists are dead files: verify with `launchctl print gui/$(id -u)/com.loops.<name>`, then delete them.
- **`loopctl`'s global flags use two deliberately different argparse flavors — do not "simplify" them into one.** `_common_parser(hidden=True)` builds the top-level parser's copy (real defaults, `help=SUPPRESS`); `_common_parser(suppress_defaults=True)` builds every subparser's copy (`default=argparse.SUPPRESS`, normal help). The reason is mechanical: argparse's `_SubParsersAction.__call__` parses a verb's trailing tokens into a **fresh** namespace and then copies every key of it onto the outer one — so if a subparser's `--root` carries a real default, `loopctl --root R status` resolves `R` correctly and then silently clobbers it back to `_default_root()`. That shipped once and meant `--root <sandbox> install <loop>` bootstrapped a launchd job in the **live** fleet at exit 0. `serve` additionally takes an `include_from=False` flavor (the console always reads `loops.d`), and `main()` rejects a pre-verb `--from` with `serve` because the top-level parser's `--from` is hidden and would otherwise swallow it. Pinned by `TestGlobalFlagPlacement` (both placements, every flag).
- **An imported loop's proposed precheck lines are emitted COMMENTED, and `[read-only?]` is an advisory hint — not a guarantee and not a boundary.** `precheck.sh` is trusted unsandboxed bash (see the invariant above), so the only real safety property is that the line is commented out; the classifier's label is a heuristic with known residual escapes (enumerated in `docs/SKILL_IMPORT.md` §6). Two adversarial rounds closed `git status && rm -rf`, `ls | xargs rm -rf`, `cat <payload>/etc/hosts` and `&`/`$( )` splitting; the enumeration keeps yielding, which is why the answer is "a human reads every line before uncommenting", not "tighten the regex once more".
- **Generated pages must fetch NOTHING on load** — dashboard and report pages alike (the reports screen was retired 2026-08-02). The rule is about *references the browser dereferences*, not the substring `http://`: no webfonts, no CDN scripts, no `<link rel=stylesheet>`, no `@import`/`url()` at a remote host, no `src=`/`srcset` off-box (including protocol-relative `//cdn/x.js`, which contains no `http://` at all). Inline everything; `data:` URIs are fine. **Navigation `<a href="https://…">` is fine** — following a link is user-initiated. So is a URL sitting in escaped report text, which real findings genuinely contain (a probe reporting `http://127.0.0.1:9/dead` refused a connection), and so is `xmlns="http://www.w3.org/2000/svg"`, which is an identifier, not an address. Enforced by `tests/html_selfcontained.py` (itself tested by `tests/test_html_selfcontained.py` — a scanner nobody tests passes everything vacuously). The older "no `http://` substring anywhere" phrasing was both too strict and too loose; do not reinstate it.
