# hello-loop — pilot example (type=agent). Permanent regression fixture
# under examples/; NEVER installed to launchd (docs/INTERFACES.md §1).
# See README.md for how to run it supervised, and SPEC.md for the intake
# interview this loop was built from.
name=hello-loop
description="Pilot: scans the fixture world/ for open TODO markers and reports them as findings"
type=agent
engine=codex
schedule=daily:09:00
# workdir=$LOOPS_ROOT
timeout_s=300
# enabled=true
# retention_days=30
# retry_transient=1
perm_fs_write=report_only
perm_network=none
perm_local_exec=none
perm_remote_mutation=none
# exec_allowlist=
# credential_env=
# remote_mutation_justification=
notes="Pilot/regression fixture (docs/HARNESS_PLAN.md step 6). Report-only floor is sufficient: precheck.sh already gathered everything the engine needs to interpret."
